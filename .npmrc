# @vercel/analytics가 선언한 '선택적' SvelteKit peer를 npm이 Vite 5(Vitest)와 충돌로 잡아
# ERESOLVE가 나서 추가. 이 프로젝트는 Svelte 미사용 — 선택적 peer 무시가 안전.
# Vercel 빌드(npm install)도 이 설정을 읽어 동일하게 설치된다.
legacy-peer-deps=true
